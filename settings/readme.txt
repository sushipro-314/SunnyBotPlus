Meaning for default_server_config.json:
Default server JSON for every server that is set by default upon starting and generating server data.

Meaning for 8ball.json:
8Ball.json is the default 8ball values for configuring the magic eight ball feature under the Fun cog